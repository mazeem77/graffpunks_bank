const ClanModel = require('../../models/clan');
const CharacterModel = require('../../models/character');

const resetProximoSeasonJob = (job, done) => {
  done();
};

module.exports = resetProximoSeasonJob;
